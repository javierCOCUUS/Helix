# Helix

Starter repository for the Helix project.

## Quick start

1. Create and activate a virtual environment.
2. Install dependencies in `requirements.txt`.
3. Run tests from the project root.

## Project structure

- `src/`: application code
- `tests/`: test suite
